mybatis:
  config-location: classpath:/mybatis-config.xml
  mapper-locations: classpath:/mapper/**/*.xml
datasource:
  username: root
  password: root
  url: jdbc:mysql://localhost:3306/db-mybatis
  driver: com.mysql.jdbc.Driver
